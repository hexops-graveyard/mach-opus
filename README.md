## Deprecated

This project is deprecated. We suggest using the build.zig Zig package (which is still maintained, https://code.hexops.com/hexops/opus) and using the C API directly in your application.
